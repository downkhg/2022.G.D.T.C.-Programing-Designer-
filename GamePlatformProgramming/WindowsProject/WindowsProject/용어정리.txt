#기초용어
함수호출(Function Call):
CallBack:
API(Application Programming Interface):
WCARH(Uni-Code:UTF-8):
동적배열:
(정적)배열:

#객체지향언어(C++/Java/C#)
객체(포인터):
인스턴스(동적할당메모리):
스마트포인터:
상속:

#컴퓨터구조(컴퓨터과학)
CPU:
RAM:
ROM(HDD):

#플랫폼(운영체제론)
OS(Operation System):
GUI(Graphic User Interface):
드라이버(SW):
Window(OS):
WinAPI:
메시지:
가상머신(JVM,CRL):
가비지컬렉션(메모리관리):
스레드:

#메모리
스택(지역변수,전역변수):
힙(동적할당메모리):
메모리페이징:

#자료구조
배열:
큐: 뒤에서 넣고 앞에서 뺀다. 입력된 순서대로 빠진다.
스택: 뒤에서 넣고 뒤에서뺍니다. 먼저들어온것이 가장 나중에 빠진다.
