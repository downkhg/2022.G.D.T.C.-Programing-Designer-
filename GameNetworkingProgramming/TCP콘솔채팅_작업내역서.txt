1.서버
1)서버에 포트를 지정한다.
2)서버는 클라이언트의 접속을 대기한다.
3)클라이언트가 접속하면 또 다른 클라이언트를 대기시킨다.
4)클라이언트의 접속하면, 버퍼를 준비하고 전송된 값을 읽는다.
5)읽은 값을 다른 클라이언트에게 전송한다.

2.클라이언트
1)서버의 주소값과 포트를 이용하여 접속한다.
2)접속 후 메세지를 서버에 전송한다.
3)다른클라이언트가 받은 메세지가 있다면, 스트림을 읽는다.
4)읽은 스트림의 값을 문자열로 변환한다.
5)메세지를 출력한다.